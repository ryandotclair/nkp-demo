apiVersion: config.gatekeeper.sh/v1alpha1
kind: Config
metadata:
  name: config
  namespace: nkp-flux-provisioned-phvwv-jjxpb
spec:
  sync:
    syncOnly:
      - group: "policy"
        version: "v1" # Or "v1beta1" depending on your cluster version (see Step 2)
        kind: "PodDisruptionBudget"
